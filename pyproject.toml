[project]
name = "turbu"
version = "0.1.0"
description = "Active nematic turbulence simulation — Lavi et al. PRR 2026"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.24",
    "matplotlib>=3.7",
]
